{
  "name": "pitch-pair",
  "version": "0.1.0",
  "private": true,
  "description": "Japanese pitch-accent minimal pair trainer with an FSRS-6 scheduler",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "node --test --test-reporter=spec \"test/*.test.js\"",
    "simulate": "node bin/simulate.js",
    "serve": "python3 -m http.server 8000"
  },
  "engines": {
    "node": ">=20"
  }
}
